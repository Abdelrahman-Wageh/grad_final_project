# Smartino Backend - Minimal Requirements (NO AI MODELS)
# This file contains ONLY the dependencies needed for DRY_RUN mode

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart==0.0.6
pydantic>=2.10.0
pydantic-settings>=2.6.0

# System Monitoring
psutil>=5.9.0

# Audio Processing (for placeholder TTS)
numpy>=1.26.0
scipy>=1.14.0

# Image Processing (for CV placeholder)
Pillow>=10.4.0

# YAML config (for NLU rules)
PyYAML==6.0.1

# Logging
python-json-logger==2.0.7

# Testing (optional)
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
