# Build configuration for Hive code generation
targets:
  $default:
    builders:
      hive_generator:hive_generator:
        enabled: true
        options:
          # Generate adapters for all @HiveType annotated classes
          generate_for:
            - lib/core/config/dev_settings.dart
            - lib/models/**/*.dart
